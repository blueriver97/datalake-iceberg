# OpenLineage 설정 백업 (필요 시 spark-submit --conf로 추가)
# --conf spark.extraListeners=io.openlineage.spark.agent.OpenLineageSparkListener
# --conf spark.openlineage.transport.type=http
# --conf spark.openlineage.transport.url=http://openlineage:5000
# --conf spark.openlineage.transport.endpoint=/api/v1/lineage
# --conf spark.openlineage.transport.auth.type=api_key
# --conf spark.openlineage.transport.auth.apiKey=changeme
# --conf spark.openlineage.namespace=prod
# --conf spark.openlineage.appName=spark.prod.<dag_id>
