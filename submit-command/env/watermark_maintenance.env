# YARN 드라이버에 배포되는 .env (--files로 전달)
# watermark_maintenance는 Settings를 사용하지 않고 CLI 인자만 사용
# 최소한의 환경변수만 정의

SPARK_HOME=/opt/spark
HADOOP_CONF_DIR=/opt/hadoop/etc/hadoop
PYSPARK_PYTHON=./environment/bin/python3
